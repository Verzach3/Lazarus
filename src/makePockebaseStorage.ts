import pocketbaseEs from "pocketbase";


function makePocketbaseStorage() {
  const client = new pocketbaseEs("http://127.0.0.1:8090");

  return {
    
  }
}




export default makePocketbaseStorage;